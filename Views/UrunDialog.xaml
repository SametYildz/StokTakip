<Window x:Class="StokTakip.Views.UrunDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ürün Bilgileri" Height="420" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Label Content="Ürün Adı"/>
                <TextBox x:Name="txtAd"/>

                <Label Content="Kategori"/>
                <TextBox x:Name="txtKategori"/>

                <Label Content="Fiyat (₺)"/>
                <TextBox x:Name="txtFiyat"/>

                <Label Content="Stok Adedi"/>
                <TextBox x:Name="txtStok"/>

                <Label Content="Minimum Stok (uyarı eşiği)"/>
                <TextBox x:Name="txtMinStok"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="İptal" Width="80" Margin="0,0,8,0"
                    Click="Iptal_Click" Padding="8,6"/>
            <Button Content="Kaydet" Width="80"
                    Click="Kaydet_Click" Padding="8,6"
                    Background="#2196F3" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>